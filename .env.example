# Copy to .env and fill the values
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role key (keep secret, use Vercel Secrets in production)
SUPABASE_SERVICE_ROLE_KEY=
# Optional simple API key to protect server mutating endpoints (POST/PUT/DELETE)
API_KEY=
# Bucket name used for campaign images
SUPABASE_BUCKET=campaign-images
